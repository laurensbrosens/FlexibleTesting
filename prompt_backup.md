I want you to refactor @FlexibleTesting.Tasks/FlexibleTestingTask.cs , try not to change the workings of the application. There is a lot of duplicate code. Try      
and apply the following changes. Go in waves and keep track of where you were (in this @prompt.md file) so I can jump in if anything goes wrong. Stop before applying the next point so I can review. Here are the different things to apply:
1. Remove magic strings everywhere: use typeof/nameof if necessary, better to use roslyn provided types if possible
2. Always use {} with every if/else/elseif, no single line statements
3. Use switches for the CodeAnalysis Syntaxes instead of separate if statements if possible, with comments on the cases that are unimplemented    
4. Refactor @FlexibleTesting.Tasks/MockableParameter.cs @FlexibleTesting.Tasks/MockableSpec.cs @FlexibleTesting.Tasks/MockableKind.cs @FlexibleTesting.Tasks/FlexibleTestingInstructions.cs : Create a single class instead of these records, keep different hashsets for properties/fields/methods etc. instead of the MockableKind enum. Use ISymbol and similar interfaces to save instead. This does mean that everywhere these records are used need to be changed as well. Take into account that the code cannot directly compare the ISymbols, look at the example in @README.md where a private interface is used in order to bypass linking to a private method (only the signature should be compared for the Overwrates.MakePublic). It is best to create a helper method in a new class to compare the ISymbols for this usecase instead of the existing records.
5. Split the FlexibleInstruction creation from Split @FlexibleTesting.Tasks/FlexibleTestingTask.cs into a new class. Do the same for the code generation part of @FlexibleTesting.Tasks/FlexibleTestingTask.cs , it needs to be uncoupled from the FlexibleInstruction creation.
6. Make everything (in the FlexibleTesting.Tasks namespace) clean and solid, reduce duplicate code/logic, don't use the type of a local variable the name of the variable, I rather have it slightly more verbose
